function getSubstring(input, start, count) {
    let inputSubstring = input.substring(start, start + count);
    console.log(inputSubstring);
}

getSubstring('ESentence', 1, 8);
getSubstring('DropWord', 4, 7);
